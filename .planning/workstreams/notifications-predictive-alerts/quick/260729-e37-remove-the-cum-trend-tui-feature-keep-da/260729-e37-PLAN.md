---
phase: 260729-e37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/generate.py
  - fixtures/README.md
  - fixtures/snapshot/cold-start-null-sections.json
  - fixtures/snapshot/partial-sections.json
  - fixtures/snapshot/cum-trend-populated.json
  - fixtures/snapshot/cum-trend-clipped-keeps-newest.json
  - rust/src/main.rs
  - rust/src/snapshot.rs
  - rust/tests/fixtures.rs
  - claude-monitor.py
  - claude_monitor/test_claude_monitor.py
autonomous: true
requirements: [QT-260729-e37]

must_haves:
  truths:
    - "claude-tui renders exactly the trends panel it rendered before 260727-krn ever shipped: one hourly bar graph, no second window-usage graph, no window-usage label, for every remaining corpus fixture and every terminal width."
    - "The daemon's socket snapshot query response no longer contains cum_trend or cum_trend_axis keys, and Monitor no longer computes or caches those values (self.cum_trend / self.cum_trend_axis are gone from Monitor.__init__ and compute_trends)."
    - "claude_monitor.core.build_cum_trend and claude_monitor.core.cum_trend_axis remain fully intact -- same signatures, same bodies, still imported and covered by their own pure-function unit tests in claude_monitor/test_claude_monitor.py -- untouched by this removal because they are reserved for a future dashboard-only feature (explicitly out of scope here)."
    - "The shared fixture corpus (fixtures/snapshot/*.json, fixtures/generate.py, fixtures/README.md) and the Rust fixture harness (rust/tests/fixtures.rs) no longer reference cum_trend or cum_trend_axis anywhere; the two fixtures that existed solely to exercise the feature (cum-trend-populated, cum-trend-clipped-keeps-newest) are deleted, not just edited."
    - "just selfcheck, cargo test (rust/), cargo clippy --all-targets -- -D warnings (rust/), and just lint (ruff) are all green after the removal, and the running daemon plus the installed claude-tui binary both reflect the change (just install, then just restart)."
  artifacts:
    - rust/src/main.rs
    - rust/src/snapshot.rs
    - rust/tests/fixtures.rs
    - fixtures/generate.py
    - fixtures/README.md
    - claude-monitor.py
    - claude_monitor/test_claude_monitor.py
  key_links:
    - "claude-monitor.py's _handle_conn socket payload dict is the sole wire producer of cum_trend/cum_trend_axis; removing its two keys is what makes rust/src/snapshot.rs's field removal safe on the wire -- the daemon simply stops sending them, and Rust's hand-walked normalizer already ignores unknown/missing wire keys by design (D-04, forward compatibility), so a stale daemon/client pair during rollout degrades gracefully instead of breaking."
    - "claude_monitor/test_claude_monitor.py's IN-01 socket-wire test (_FakeMonitor + _handle_conn, ~lines 1089-1129) asserts the exact key set of the JSON response; it must shrink in lockstep with the payload dict edit in the same task, or just selfcheck fails on this task's own change."
    - "rust/tests/fixtures.rs's check() dispatches fixture expect keys by name via a match; a fixture JSON still carrying a cum_trend/cum_trend_axis expect key after the match arms are deleted fails loudly with 'unknown expectation key' -- so the fixture JSON/generate.py edits (Task 1) must land before the Rust match-arm removal (Task 2), not after, which is why this plan orders them that way rather than doing all Rust code in one pass."
---

<objective>
Remove the cum_trend (cumulative-window-usage sparkline) feature from the Rust TUI
entirely, plus its wire-protocol plumbing in the Python daemon and its coverage in the
shared fixture corpus. The feature was added across four recent quick tasks
(260727-krn/lns/mki/nlo) but the user has decided it should be dashboard-only, never
shown in the TUI. There is currently no dashboard rendering of cum_trend
(claude_monitor/dashboard.py has zero references) -- adding that is a separate future
task, explicitly out of scope here.

Purpose: the terminal dashboard should go back to showing exactly what it showed
before 260727-krn: one hourly usage-trend bar graph, nothing else in that panel. The
generic pure functions that compute the cum_trend series (claude_monitor.core's
build_cum_trend / cum_trend_axis) are reserved for a future dashboard feature and stay
completely untouched.

Output: rust/src/main.rs and rust/src/snapshot.rs have no cum_trend rendering, struct
fields, or normalization; the daemon's socket payload and claude-monitor.py's Monitor
no longer carry cum_trend/cum_trend_axis; the shared fixture corpus and its README no
longer reference the feature; all four verification gates (selfcheck, cargo test,
clippy, ruff) are green; the running daemon and the installed claude-tui binary both
reflect the change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notifications-predictive-alerts/STATE.md

# Regions to read before editing (do NOT re-read a range already seen this session):
@fixtures/generate.py   # F["cold-start-null-sections"] ~lines 50-59; F["partial-sections"] ~lines 70-83; F["cum-trend-populated"] ~lines 170-184; _CUM_CLIMB/_CUM_FUTURE_BLANK/_CUM_SPARKLINE + F["cum-trend-clipped-keeps-newest"] ~lines 186-220; write loop ~lines 301-307 (regenerates every fixtures/snapshot/*.json from F/RAW, but never DELETES an orphaned file whose F entry was removed)
@fixtures/README.md   # Expectations table ~lines 42-49, cum_trend/cum_trend_axis rows at 46-47
@rust/src/snapshot.rs   # wire-contract doc comment ~lines 36-47; Snapshot struct + cum_trend/cum_trend_axis fields ~lines 247-271; Snapshot::from_value ~lines 285-305; normalize_trends ~lines 450-468 (KEEP, still used by trends); normalize_trend_axis ~lines 470-484 (KEEP, still used by trend_axis)
@rust/src/main.rs   # snapshot_cum_trend ~lines 764-772; trends_panel_height ~lines 781-794; clip_to_newest + its doc comment ~lines 894-950; draw_trends ~lines 952-1009 (graph_width ~977, mut graph ~979-982, cum block ~986-993); test module cum tests ~lines 1402-1537 (buffer_text ~1402-1406, render_trends_rows ~1408-1423, cum_trend_adds_a_second_graph_below_the_hourly_bars ~1425-1461, cum_trend_absent_is_a_true_no_op_on_render_and_layout ~1463-1477, cum_trend_sparkline_clipping_keeps_the_newest_columns_not_the_oldest ~1479-1510, clip_to_newest_drops_the_trailing_future_blank_run_not_the_real_data ~1512-1537)
@rust/tests/fixtures.rs   # check() match ~lines 140-150 (cum_trend/cum_trend_axis arms at 144-145); check_axis ~lines 260-287 (dead once the cum_trend_axis arm is gone -- confirmed its only caller)
@claude-monitor.py   # Monitor.__init__ ~lines 74-75; compute_trends ~lines 390-391; _handle_conn snapshot dict ~lines 670-679
@claude_monitor/test_claude_monitor.py   # imports ~lines 36,39-40 (build_cum_trend, cum_trend_axis, CUM_TREND_INTERVAL -- DO NOT TOUCH); pure-function unit tests ~lines 620-664 (DO NOT TOUCH -- test core.py's retained functions directly); IN-01 socket-wire test ~lines 1089-1129 (_FakeMonitor fields at 1099-1100, key-set assert at 1121, value asserts at 1126-1127 -- THESE change)
@justfile   # selfcheck ~37-38, rust-test ~74-75, rust-lint ~78-79, lint ~40-41, install ~71-72, restart ~16-20

Reuse, do NOT reimplement or refactor beyond deletion:
- normalize_trends / normalize_trend_axis (rust/src/snapshot.rs) -- both stay exactly as they are, still consumed by the (untouched) trends/trend_axis wire fields
- check_trends(section, expected, name) (rust/tests/fixtures.rs) -- keep its Section+name signature even though it drops to one caller ("trends") after this task; this is a deletion task, not a signature cleanup
- trend_graph_lines, TREND_ROWS, spark_levels (rust/src/main.rs) -- the original hourly bar chart's own rendering code was never modified to add cum_trend and must not be touched to remove it either
- claude_monitor.core.build_cum_trend / claude_monitor.core.cum_trend_axis (claude_monitor/core.py) -- explicitly out of scope; only the daemon's *calls* to them (in claude-monitor.py, not core.py) are removed

Do not touch: claude_monitor/core.py in any way, its build_cum_trend/cum_trend_axis
functions, CUM_TREND_INTERVAL, TREND_INTERVAL, POLL_INTERVAL, or their unit tests in
claude_monitor/test_claude_monitor.py (~lines 600-664). No new dependency, no new
config knob.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop cum_trend from the shared fixture corpus</name>
  <files>fixtures/generate.py, fixtures/README.md, fixtures/snapshot/cold-start-null-sections.json, fixtures/snapshot/partial-sections.json, fixtures/snapshot/cum-trend-populated.json, fixtures/snapshot/cum-trend-clipped-keeps-newest.json</files>
  <action>
    Corpus first, before any Rust or Python code changes, so every later task's
    `cargo test` stays green throughout (rust/tests/fixtures.rs's match arms and
    rust/src/snapshot.rs's fields are untouched in this task -- removing the fixture
    JSON's cum_trend/cum_trend_axis expect keys just means those match arms go
    unreached, which compiles and passes fine).

    In fixtures/generate.py:
    1. F["cold-start-null-sections"]: remove `"cum_trend": None, "cum_trend_axis": None`
       from its "wire" dict, and `"cum_trend": "absent", "cum_trend_axis": "absent"`
       from its "expect" dict. Keep the rest of the entry (usage/trends/heatmap/sessions
       null-section coverage) unchanged.
    2. F["partial-sections"]: remove `"cum_trend": [42],` from "wire" and
       `"cum_trend": "malformed",` from "expect". Keep the rest (D-02 sibling-section
       independence coverage via heatmap/trends/sessions) unchanged.
    3. Delete the entire F["cum-trend-populated"] entry.
    4. Delete the entire F["cum-trend-clipped-keeps-newest"] entry, plus the
       `_CUM_CLIMB`, `_CUM_FUTURE_BLANK`, `_CUM_SPARKLINE` module-level variables and
       their preceding comments -- confirmed by grep that entry is their only
       consumer. Deleting cum-trend-populated does not lose OSC-52/BEL sanitization
       coverage: hostile-controls-in-trend-rows already pins the identical hostile-row
       case through the same normalize_trends path.

    In fixtures/README.md: remove the `cum_trend` and `cum_trend_axis` rows from the
    Expectations table.

    Regenerate: run `python3 fixtures/generate.py` from the repo root. This rewrites
    every fixtures/snapshot/*.json listed in F/RAW in place (including
    cold-start-null-sections.json and partial-sections.json, now without cum keys) but
    does NOT delete files whose F entry was removed -- explicitly `git rm
    fixtures/snapshot/cum-trend-populated.json
    fixtures/snapshot/cum-trend-clipped-keeps-newest.json` afterward. Confirm with
    `git status --short fixtures/snapshot/` that exactly those two files are deleted
    and cold-start-null-sections.json / partial-sections.json are modified, with no
    other fixture's generated content drifting. The corpus count assertion
    (`the_corpus_is_non_empty_and_well_formed`, `>= 15`) still holds at 24 fixtures
    (26 minus the 2 removed) -- no test code change needed for this.
  </action>
  <verify>
    <automated>python3 fixtures/generate.py && git status --short fixtures/snapshot/ && cd rust && cargo test --quiet && cd .. && just selfcheck</automated>
  </verify>
  <done>fixtures/generate.py, fixtures/README.md, and the regenerated JSON corpus carry
  no cum_trend/cum_trend_axis wire or expect keys anywhere; cum-trend-populated.json and
  cum-trend-clipped-keeps-newest.json are deleted (git rm, not just edited); `cargo
  test` and `just selfcheck` are both still green (Rust code and Python daemon code are
  untouched in this task, so this only proves the corpus edit itself is self-consistent
  and every remaining fixture still normalizes correctly).</done>
</task>

<task type="auto">
  <name>Task 2: Remove cum_trend rendering and wire plumbing from the Rust crate</name>
  <files>rust/src/main.rs, rust/src/snapshot.rs, rust/tests/fixtures.rs</files>
  <action>
    One compile unit, one task: main.rs, snapshot.rs and fixtures.rs's match arms must
    change together, since fixtures.rs references snapshot.cum_trend directly.

    In rust/src/snapshot.rs:
    1. Update the wire-contract doc comment near the top of the file: delete the
       `"cum_trend": [string] | null,` and `"cum_trend_axis": [string] | null` lines
       from the JSON shape it documents.
    2. Remove the `cum_trend: Section<Vec<String>>` field and its preceding doc
       comment, and the `cum_trend_axis: Option<Vec<String>>` field and its preceding
       doc comment, from the `Snapshot` struct.
    3. In `Snapshot::from_value`, remove the `cum_trend: normalize_trends(...)` and
       `cum_trend_axis: normalize_trend_axis(...)` lines from the struct literal.
       Leave `normalize_trends` and `normalize_trend_axis` themselves completely
       unmodified -- both remain the normalizers for `trends` and `trend_axis`.

    In rust/src/main.rs:
    4. Remove the `snapshot_cum_trend` function and its preceding doc comment.
    5. In `trends_panel_height`, remove the `if let Some(cum) = snapshot_cum_trend(app)
       { ... }` block and revert `let mut left = ...` to `let left = ...` (no longer
       mutated).
    6. Remove the `clip_to_newest` function and its long preceding doc comment
       entirely -- it exists solely to clip the cum_trend sparkline and has no other
       caller.
    7. In `draw_trends`: remove the `let graph_width = ...` line (its only consumer
       was the cum block being removed); change `let mut graph = trend_graph_lines(...)`
       to `let graph = trend_graph_lines(...)` (no longer mutated after this); remove
       the `if let Some(cum) = snapshot_cum_trend(app) { ... }` block that pushes the
       "window usage" label line and extends the graph with the clipped cum rows.
    8. In the `#[cfg(test)] mod tests` block: remove
       `cum_trend_sparkline_clipping_keeps_the_newest_columns_not_the_oldest` and
       `clip_to_newest_drops_the_trailing_future_blank_run_not_the_real_data` outright.
       Remove `cum_trend_adds_a_second_graph_below_the_hourly_bars` outright too. For
       `cum_trend_absent_is_a_true_no_op_on_render_and_layout`: it is the only
       automated assertion pinning the pre-cum height formula
       (`TREND_ROWS + trends_len - 1 + 2`) -- do not just delete it. Replace it with a
       new test, e.g. `trends_panel_height_matches_the_bar_chart_plus_border_formula`,
       that builds an app from a plain trends-only wire (`{"trends":["..",
       "today 1M/hr"]}`) and asserts only
       `trends_panel_height(&app) == (TREND_ROWS + trends_len - 1 + 2) as u16`. Drop
       the "window usage" label-leak assertions from the old test -- they have no
       target left to assert against.
    9. Remove the now-dead helper functions `buffer_text` and `render_trends_rows` --
       confirm their only callers were the three removed cum tests before deleting
       (they are not used by any surviving test).
    10. Do not touch `trend_graph_lines`, `TREND_ROWS`, `spark_levels`, or
        `the_trend_graph_draws_every_tick_the_daemon_sends_at_its_own_row` -- none of
        these were ever modified to add cum_trend.

    In rust/tests/fixtures.rs:
    11. Remove the `"cum_trend" => check_trends(&snapshot.cum_trend, expected,
        "cum_trend")?,` and `"cum_trend_axis" => check_axis(&snapshot.cum_trend_axis,
        expected, "cum_trend_axis")?,` match arms from `check()`.
    12. Remove the now-dead `check_axis` function -- confirmed it has no other caller;
        `trend_axis` has no fixture `expect` key at all today, only `cum_trend_axis`
        used it.
    13. Leave `check_trends`'s `(section, expected, name)` signature unchanged even
        though it now has a single caller (`"trends"`) -- this task deletes cum_trend,
        it does not refactor the surviving generalized helper back to a
        single-purpose one.
  </action>
  <verify>
    <automated>cd rust && cargo test --quiet && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>`cargo test` is green: the folded
  `trends_panel_height_matches_the_bar_chart_plus_border_formula` test passes and the
  three cum-specific tests are gone. `cargo clippy --all-targets -- -D warnings` is
  clean -- dead_code is warn-by-default, so this mechanically catches any leftover
  unused function (snapshot_cum_trend, clip_to_newest, buffer_text,
  render_trends_rows, check_axis) or unused variable (graph_width) that the manual
  removal missed. The `Snapshot` struct has exactly usage/trends/trend_axis/heatmap/
  sessions fields. `trend_graph_lines`, `TREND_ROWS`, `normalize_trends`, and
  `normalize_trend_axis` are byte-for-byte unchanged from before this task.</done>
</task>

<task type="auto">
  <name>Task 3: Remove cum_trend from the Python daemon's wire payload, then verify and deploy</name>
  <files>claude-monitor.py, claude_monitor/test_claude_monitor.py</files>
  <action>
    In claude-monitor.py:
    1. `Monitor.__init__`: remove the `self.cum_trend = None` and
       `self.cum_trend_axis = None` lines (with their trailing comments).
    2. `compute_trends`: remove `self.cum_trend = core.build_cum_trend(records, now)`
       and `self.cum_trend_axis = core.cum_trend_axis(records, now)`. This only stops
       the daemon from *calling* core.build_cum_trend/core.cum_trend_axis -- those
       functions themselves, in claude_monitor/core.py, are not touched by this task
       and remain fully intact for a future dashboard feature.
    3. `_handle_conn`'s socket snapshot dict: remove the `"cum_trend": mon.cum_trend,`
       and `"cum_trend_axis": mon.cum_trend_axis,` lines.

    In claude_monitor/test_claude_monitor.py (the IN-01 socket-wire test only -- do
    NOT touch the `build_cum_trend`/`cum_trend_axis`/`CUM_TREND_INTERVAL` imports near
    the top of the file, nor the pure-function unit tests around lines 620-664 that
    call `build_cum_trend(...)`/`cum_trend_axis(...)` directly against
    `claude_monitor.core` -- those test core.py's retained functions and are
    unaffected by a wire-payload change):
    4. In `_FakeMonitor.__init__`, remove `self.cum_trend = ["cum1"]` and
       `self.cum_trend_axis = ["100%", "", "", "57%", "", "", "", "0"]`.
    5. In the IN-01 assertions: shrink
       `assert set(_snapshot.keys()) == {"heatmap", "sessions", "usage", "trends",
       "trend_axis", "cum_trend", "cum_trend_axis"}` to drop `"cum_trend"` and
       `"cum_trend_axis"` from the expected set. Remove the
       `assert _snapshot["cum_trend"] == _mon.cum_trend` and
       `assert _snapshot["cum_trend_axis"] == _mon.cum_trend_axis` lines.

    Full verification gate (all four must be green/clean):
    6. `just selfcheck`
    7. `cd rust && cargo test --quiet`
    8. `cd rust && cargo clippy --all-targets -- -D warnings`
    9. `just lint` (ruff)

    Deploy so the change is actually live, not just committed:
    10. `just install` -- rebuilds and installs the `claude-tui` binary. The installed
        binary is a copy, not a symlink, so `cargo build`/`cargo test` alone (Task 2)
        changes nothing a user can see until this runs.
    11. `just restart` -- relaunches the Python daemon (claude-monitor.py is
        symlinked, but Python does not hot-reload a running process).
    12. `just status` to confirm the daemon process is running after restart.
  </action>
  <verify>
    <automated>just selfcheck && (cd rust && cargo test --quiet) && (cd rust && cargo clippy --all-targets -- -D warnings) && just lint</automated>
  </verify>
  <done>All four gates (selfcheck, cargo test, clippy, ruff) are green/clean. The
  socket "snapshot" query response no longer contains cum_trend or cum_trend_axis keys
  (proven by the updated IN-01 test passing). `Monitor.__init__` and `compute_trends`
  no longer reference cum_trend/cum_trend_axis. `claude_monitor/core.py` and its
  build_cum_trend/cum_trend_axis unit tests are byte-for-byte unchanged from before
  this task. `just install` and `just restart` both completed successfully and `just
  status` shows a running daemon PID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Daemon socket -> Rust TUI wire payload | unchanged boundary, shrinking payload (two fewer keys); already-trusted crossing point, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e37-01 | Tampering | shrinking JSON socket payload (cum_trend/cum_trend_axis keys removed) | low | accept | removing keys from an already-trusted-boundary-crossing payload adds no new surface; Rust's hand-walked normalizer already tolerates unknown/missing keys by design (D-04/forward-compat), so a stale-daemon/new-client or new-daemon/stale-client pair during rollout degrades to Section::Absent, never panics |
| T-e37-02 | Denial of Service | rust/tests/fixtures.rs `check()` hitting `unknown expectation key` if corpus and Rust code drift out of order | low | mitigate | this plan's task ordering (corpus first, Rust code second) makes the drift window impossible: fixture JSON never carries cum_trend/cum_trend_axis expect keys by the time the match arms are removed |
| T-e37-SC | Tampering | npm/pip/cargo installs | low | accept | no new dependency is added or removed by this task |
</threat_model>

<verification>
- Task 1: `python3 fixtures/generate.py` + `git status --short fixtures/snapshot/` show
  exactly two deletions and two modifications; `cargo test` and `just selfcheck` stay
  green with no Rust/Python code changes yet.
- Task 2: `cargo test` and `cargo clippy --all-targets -- -D warnings` are both
  clean after removing cum_trend from rust/src/main.rs, rust/src/snapshot.rs, and
  rust/tests/fixtures.rs together.
- Task 3: `just selfcheck`, `cargo test`, `cargo clippy --all-targets -- -D warnings`,
  and `just lint` are all green/clean after removing cum_trend from the daemon's wire
  payload; `just install` and `just restart` both succeed; `just status` shows a
  running daemon.
- Whole-plan: no file under rust/src, rust/tests, fixtures/, claude-monitor.py, or
  claude_monitor/test_claude_monitor.py's IN-01 test references cum_trend/cum_trend_axis
  after Task 3, except as an unavoidable substring inside the untouched
  build_cum_trend/cum_trend_axis identifiers in claude_monitor/core.py and its own
  pure-function tests.
</verification>

<success_criteria>
- The Rust TUI's trends panel renders identically to how it rendered before
  260727-krn: one hourly bar graph, nothing else.
- The daemon's socket snapshot payload no longer carries cum_trend/cum_trend_axis, and
  Monitor no longer computes or caches them.
- claude_monitor.core.build_cum_trend and claude_monitor.core.cum_trend_axis, plus
  their existing unit tests, are completely untouched -- reserved for a future
  dashboard-only feature.
- The shared fixture corpus, its README, and the Rust fixture harness carry no
  cum_trend/cum_trend_axis references; the two feature-only fixtures are deleted.
- selfcheck, cargo test, clippy (-D warnings), and ruff are all green/clean.
- The running daemon and the installed claude-tui binary both reflect the change
  (just install + just restart both ran).
- No new dependency, no new poll, no new config knob.
</success_criteria>

<output>
Create `.planning/workstreams/notifications-predictive-alerts/quick/260729-e37-remove-the-cum-trend-tui-feature-keep-da/260729-e37-SUMMARY.md` when done
</output>
