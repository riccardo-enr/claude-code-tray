---
phase: 06-notification-control-config
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - claude-monitor.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05]

must_haves:
  truths:
    - "Each of the four event types can be switched on and off from the tray menu, and the very next event honors the change with no restart (ROADMAP SC1, CFG-01)."
    - "A single 'Mute all' tray toggle silences every notification while the tray rows and icon badge keep working (ROADMAP SC2, CFG-02)."
    - "The badge threshold is chosen from four fixed presets in the tray menu, never free text, completing ROADMAP SC5/CFG-05 (Plan 1 built the data model; this plan builds the menu)."
    - statement: "The four event-toggle checkbox rows render in a fixed, code-literal order (waiting, done, 5h, 7d) on every rebuild_menu() call -- the order comes from iterating a literal list of (label, key) pairs, not dict iteration (CFG-01 ordering probe)."
      verification: backstop
    - statement: "The mute-all checkbox's toggled handler writes only self.config['mute_all'] and never touches any notify_* key -- so turning mute off after on restores exactly the per-event states as they were, with no re-sync step (D-04, CFG-02 idempotency probe)."
      verification: backstop
    - statement: "The four badge-threshold radio items render in the same fixed ascending order (70%, 80%, 90%, 95%) on every rebuild -- iterating THRESHOLD_CHOICES directly, never sorted or reversed (CFG-05 ordering probe)."
      verification: backstop
  artifacts:
    - "claude-monitor.py: Monitor.on_notif_toggle(item, key) -- CheckMenuItem 'toggled' handler for mute_all + the four event keys"
    - "claude-monitor.py: Monitor.on_threshold_toggle(item, val) -- RadioMenuItem 'toggled' handler, ignores the losing-active fire"
    - "claude-monitor.py: Monitor.notif_submenu() -- builds the single 'Notifications' submenu (mute-all + 4 event checkboxes, not flat top-level rows, D-03) plus the nested 'Badge threshold' submenu, fresh from self.config every call"
    - "claude-monitor.py: rebuild_menu() -- new 'Notifications' MenuItem appended after the dashboard item"
  key_links:
    - "notif_submenu() reads self.config (Plan 1) to set every widget's initial state -- Monitor.config must already be populated, which Plan 1 Task 2 guaranteed by ordering it before the first rebuild_menu() call."
    - "Every toggle handler follows the same 'mutate self.config, save_config(self.config), rebuild_menu()' sequence already used by apply_usage/handle/on_click -- no new state-management idiom is introduced."
    - "set_active() on every new widget MUST run before its 'toggled' signal is connected, or the initial state-sync itself fires the handler and forces a spurious save+rebuild on every single menu build."
    - "notif_allowed(kind, self.config) (Plan 1) is the ONLY place mute/per-event state feeds notification delivery -- this plan must not add a second read path that could diverge from it (e.g. gating build_label or the '!' attention counter on mute_all)."

  prohibitions:
    - statement: "MUST NOT let 'Mute all' suppress anything beyond desktop notification delivery -- the tray menu rows, the icon badge, and the session '!' attention indicator must keep working exactly as before while muted (ROADMAP SC2)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the badge-threshold selection grow into free-text or arbitrary numeric entry -- only the four fixed presets are acceptable input, matching D-05's explicit rejection of a text-entry widget."
      status: resolved
      verification: judgment
---

<objective>
Build the tray menu surface CFG-01/02/05 are actually judged on: one "Notifications"
submenu (mute-all + the four event toggles, not flat top-level rows, per D-03) and a
nested "Badge threshold" submenu (four radio presets), both reading and writing the
config layer Plan 1 built.

Purpose: the user-observable half of CFG-01, CFG-02, and CFG-05 -- ROADMAP Success
Criteria 1, 2, and 5.
Output: two new `Gtk.CheckMenuItem`/`Gtk.RadioMenuItem` groups wired into `rebuild_menu`,
persisting through the existing `self.config` / `save_config` seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/notifications-predictive-alerts/ROADMAP.md
@.planning/workstreams/notifications-predictive-alerts/STATE.md
@.planning/workstreams/notifications-predictive-alerts/REQUIREMENTS.md
@.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-CONTEXT.md
@.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-PATTERNS.md
@.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-01-SUMMARY.md
@claude-monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notifications submenu -- mute-all + the four event checkboxes</name>

  <read_first>
    - `claude-monitor.py` -- `Monitor.on_click` and `Monitor.rebuild_menu` in full (their exact line numbers have shifted from Plan 1's edits; locate them by name). Note the `dash` `MenuItem` block, and the final `Gtk.SeparatorMenuItem` + "Quit monitor" block that closes `rebuild_menu`.
    - `06-01-SUMMARY.md` -- the final signatures/locations of `self.config`, `save_config`, `DEFAULT_CONFIG`, `NOTIF_KEYS` (config keys: `notify_waiting`, `notify_done`, `notify_5h`, `notify_7d`, `mute_all`).
    - `.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-CONTEXT.md` -- D-03 (submenu shape), D-04 (mute wins at the gate, not in the UI), "Specific Ideas" (the menu sketch).
    - `.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-PATTERNS.md` -- "Notifications submenu + threshold radio items" section, the `rebuild_menu` teardown-and-rebuild idiom.
  </read_first>

  <files>claude-monitor.py</files>

  <action>
    Add `Monitor.on_notif_toggle(self, item, key)`, placed directly after `on_click` and
    before `rebuild_menu`: `self.config[key] = item.get_active()`, then
    `save_config(self.config)`, then `self.rebuild_menu()` -- the same "mutate state,
    persist, redraw" sequence `apply_usage` / `handle` / `on_click` already use elsewhere
    in this class. Docstring it as the shared handler for the mute checkbox and all four
    event checkboxes (they differ only in which config key `key` names).

    Add `Monitor.notif_submenu(self)`, placed directly after `on_notif_toggle`, returning a
    fresh `Gtk.Menu()` built entirely from `self.config` (no incremental diffing, matching
    `rebuild_menu`'s existing full-teardown style):
    - A `Gtk.CheckMenuItem` labeled "Mute all". Call `set_active(self.config["mute_all"])`
      BEFORE `connect("toggled", self.on_notif_toggle, "mute_all")` -- in that order. If the
      signal is connected first, the `set_active` call itself fires a `"toggled"` event and
      forces a spurious save-and-rebuild on every single menu construction.
    - A `Gtk.SeparatorMenuItem`.
    - Four `Gtk.CheckMenuItem` rows, built from a fixed, code-literal list of `(label, key)`
      pairs, in exactly this order: `("Waiting for input", "notify_waiting")`,
      `("Session finished", "notify_done")`, `("5-hour quota alert", "notify_5h")`,
      `("7-day quota alert", "notify_7d")`. Same `set_active`-before-`connect` order as the
      mute row, each connected to `self.on_notif_toggle` with its own `key`.
    - `return sub` (Task 2 extends this same method to append a nested threshold submenu
      before this return).

    Wire it into `rebuild_menu`: immediately after the existing `self.menu.append(dash)`
    line, add a `Gtk.MenuItem` labeled "Notifications", call
    `.set_submenu(self.notif_submenu())` on it, and `self.menu.append(...)` it -- landing
    between the dashboard item and the closing separator + "Quit monitor" block. Do not
    reorder or otherwise touch any existing line in `rebuild_menu`.
  </action>

  <acceptance_criteria>
    - `python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
c=[x for x in ast.walk(t) if isinstance(x,ast.ClassDef) and x.name=='Monitor'][0]
m={f.name:[a.arg for a in f.args.args] for f in c.body if isinstance(f,ast.FunctionDef)}
want={'on_notif_toggle':['self','item','key'],'notif_submenu':['self']}
sys.exit(0 if all(m.get(k)==v for k,v in want.items()) else 1)"` exits `0`.
    - `grep -c 'Gtk.CheckMenuItem' claude-monitor.py` returns at least `1` (constructed in a loop plus once for mute -- source presence is what matters, not a literal count of widget instances).
    - Fixed order (CFG-01 ordering probe): the four config keys appear inside `notif_submenu` in ascending source-position order matching waiting/done/5h/7d:
      ```
      python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
c=[x for x in ast.walk(t) if isinstance(x,ast.ClassDef) and x.name=='Monitor'][0]
f=[x for x in c.body if isinstance(x,ast.FunctionDef) and x.name=='notif_submenu'][0]
src=ast.dump(f)
keys=[\"'notify_waiting'\",\"'notify_done'\",\"'notify_5h'\",\"'notify_7d'\"]
pos=[src.find(k) for k in keys]
sys.exit(0 if all(p!=-1 for p in pos) and pos==sorted(pos) else 1)"
      ```
      exits `0`.
    - Mute-independence (D-04 / CFG-02 idempotency probe): `on_notif_toggle` writes exactly one config key, never a second hardcoded one:
      ```
      python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
c=[x for x in ast.walk(t) if isinstance(x,ast.ClassDef) and x.name=='Monitor'][0]
f=[x for x in c.body if isinstance(x,ast.FunctionDef) and x.name=='on_notif_toggle'][0]
assigns=[n for n in ast.walk(f) if isinstance(n,ast.Assign) and isinstance(n.targets[0],ast.Subscript)]
sys.exit(0 if len(assigns)==1 else 1)"
      ```
      exits `0`.
    - `notif_allowed` and `build_label`'s call sites are unchanged by this task: `grep -c 'notif_allowed(kind, self.config)' claude-monitor.py` still returns `1`, and `grep -c 'build_label(self.usage, attention, self.config\["usage_threshold"\])' claude-monitor.py` still returns `1` -- confirms mute is not wired into either path.
    - `python3 claude-monitor.py --selfcheck` exits `0` and prints `ok`.
    - `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>python3 claude-monitor.py --selfcheck</automated>
    <human-check>
      Live tray: open the tray menu -> "Notifications" submenu shows "Mute all", a
      separator, then the four event checkboxes (Waiting for input, Session finished,
      5-hour quota alert, 7-day quota alert) in that order, all checked by default.
      (1) Uncheck "Waiting for input", trigger a session entering the waiting state with
      no restart of the helper -- no popup fires, but the session still appears in the
      tray menu with a waiting status and its "!" indicator. Re-check it and trigger
      waiting again -- the popup fires (ROADMAP SC1).
      (2) Check "Mute all", trigger a waiting or done event -- no popup fires, but the
      session row, the usage rows, and the icon badge keep updating normally
      (ROADMAP SC2). Uncheck "Mute all" -- the four event checkboxes are exactly as they
      were before mute was turned on (D-04).
    </human-check>
  </verify>

  <done>`on_notif_toggle` + `notif_submenu` exist; the "Notifications" submenu (mute + four ordered event checkboxes) is wired into `rebuild_menu` after the dashboard item. `notif_allowed`/`build_label` call sites are untouched. `--selfcheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Nested "Badge threshold" radio submenu</name>

  <read_first>
    - `claude-monitor.py` -- `notif_submenu` as Task 1 just wrote it (the `return sub` line this task inserts before), and `THRESHOLD_CHOICES` / `DEFAULT_CONFIG["usage_threshold"]` from Plan 1.
    - `.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-CONTEXT.md` -- D-05 (fixed presets, no free-text entry) and "Specific Ideas".
    - `.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-PATTERNS.md` -- "USAGE_THRESHOLD constant -> configurable" section.
  </read_first>

  <files>claude-monitor.py</files>

  <action>
    Add `Monitor.on_threshold_toggle(self, item, val)`, placed directly after
    `on_notif_toggle`. First statement: `if not item.get_active(): return` -- a
    `Gtk.RadioMenuItem`'s `"toggled"` signal fires for BOTH the item losing active state
    and the item gaining it; acting on both would write `usage_threshold` twice per click
    (once to the old value, once to the new) and call `rebuild_menu()` mid-dispatch on the
    wrong pass. After the guard: `self.config["usage_threshold"] = val`, then
    `save_config(self.config)`, then `self.rebuild_menu()`.

    Extend `notif_submenu` (from Task 1): between the four event checkboxes and the
    `return sub` line, add a `Gtk.SeparatorMenuItem`, then build the nested submenu: a
    `Gtk.MenuItem` labeled "Badge threshold" whose `set_submenu()` is a fresh `Gtk.Menu()`.
    Populate it by iterating `THRESHOLD_CHOICES` directly (no `sorted()`, no `reversed()` --
    the tuple is already the fixed ascending order D-05 wants), building one
    `Gtk.RadioMenuItem` per value via the standard PyGObject group-chain idiom:
    `Gtk.RadioMenuItem.new_with_label_from_widget(group, "%d%%" % val)`, where `group` is
    `None` for the first item and the previously-created radio item for every subsequent
    one. For each: `set_active(self.config["usage_threshold"] == val)` BEFORE
    `connect("toggled", self.on_threshold_toggle, val)` -- same ordering rule as Task 1's
    checkboxes, same reason. Append the "Badge threshold" item to `sub` before the
    `return sub`.

    Do not add any text-entry widget anywhere in this submenu tree -- the four radio items
    are the entire input surface (D-05).
  </action>

  <acceptance_criteria>
    - `python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
c=[x for x in ast.walk(t) if isinstance(x,ast.ClassDef) and x.name=='Monitor'][0]
f=[x for x in c.body if isinstance(x,ast.FunctionDef) and x.name=='on_threshold_toggle'][0]
sys.exit(0 if [a.arg for a in f.args.args]==['self','item','val'] and isinstance(f.body[0],ast.If) else 1)"` exits `0` -- exact signature, first statement is the double-fire guard.
    - `grep -c 'Gtk.RadioMenuItem' claude-monitor.py` returns at least `1`.
    - `grep -c 'for val in THRESHOLD_CHOICES' claude-monitor.py` returns `1`, and `grep -c 'sorted(THRESHOLD_CHOICES)' claude-monitor.py` returns `0` -- iterates the tuple directly, no re-sort (CFG-05 ordering probe).
    - `grep -c 'Gtk.Entry' claude-monitor.py` returns `0` -- no free-text widget anywhere in the file (D-05).
    - `grep -c "if not item.get_active(): return" claude-monitor.py` returns `1` -- the double-fire guard is present exactly once.
    - `python3 claude-monitor.py --selfcheck` exits `0` and prints `ok`.
    - `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>python3 claude-monitor.py --selfcheck</automated>
    <human-check>
      Live tray: Notifications -> "Badge threshold" shows four radio options
      (70%/80%/90%/95%) with the current value (80% by default) selected. Picking a
      different value leaves exactly one radio active (no double-select) and the tray
      icon's "!" badge threshold changes accordingly on the next poll tick. Restart the
      helper -- the newly picked value is still selected (CFG-03/ROADMAP SC3, observed
      end-to-end for this specific setting) and the badge continues to follow it,
      completing ROADMAP SC5.
    </human-check>
  </verify>

  <done>`on_threshold_toggle` exists and ignores the losing-active fire; the nested "Badge threshold" submenu offers exactly the four `THRESHOLD_CHOICES` presets in fixed ascending order, radio-exclusive, defaulting to the persisted `usage_threshold`. No free-text widget exists anywhere. `--selfcheck` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tray menu (local user only) -> `self.config` -> `save_config` | Trusted local UI; the only "attacker" is the same local user, so the risk here is scope containment (does mute quietly reach further than it should), not authentication. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering / Denial of Service | mute-all UI wiring (`on_notif_toggle`, `notif_submenu`) | medium | mitigate | "Mute all" must gate ONLY notification delivery, via the existing `notif_allowed`/`emit_notif` path Plan 1 wired -- never the tray rows, `build_label`, or the session "!" attention counter. A future edit routing `self.config["mute_all"]` into any of those would silently violate ROADMAP SC2. Mitigated structurally: `on_notif_toggle` writes exactly one config key and calls `rebuild_menu()`; it has no reference to `usage_rows`, `build_label`, or the attention counter (Task 1 acceptance criteria assert the call sites are unchanged). |
| T-06-05 | Tampering | badge-threshold input surface | low | mitigate | D-05 rejects free-text entry because a GTK indicator menu handles it poorly and an out-of-range/garbage value could reach `usage_threshold`. Mitigated by construction: the only new widgets are four `Gtk.RadioMenuItem`s bound to literal `THRESHOLD_CHOICES` values -- no `Gtk.Entry` exists anywhere in the menu tree, so no arbitrary string can reach `on_threshold_toggle`. |
| T-06-SC | Tampering | supply chain | n/a | accept | No package installs in this phase. `Gtk.CheckMenuItem` / `Gtk.RadioMenuItem` ship with the already-present PyGObject/GTK3. |
</threat_model>

## Artifacts this phase produces

New symbols created by **this plan** in `claude-monitor.py`:

| Symbol | Kind | Notes |
|--------|------|-------|
| `Monitor.on_notif_toggle(item, key)` | method | checkbox `"toggled"` handler -- mute_all + 4 event keys (CFG-01, CFG-02) |
| `Monitor.on_threshold_toggle(item, val)` | method | radio `"toggled"` handler, ignores the losing-active fire (CFG-05) |
| `Monitor.notif_submenu()` | method | builds "Notifications" (+ nested "Badge threshold") fresh from `self.config` every `rebuild_menu()` call |
| "Notifications" menu item | GTK widget | new top-level tray menu entry, submenu attached, inserted after the dashboard item |
| "Badge threshold" menu item | GTK widget | nested submenu inside "Notifications" |

Consumed from Plan 1 (already created there, not new here): `self.config`, `save_config`,
`DEFAULT_CONFIG`, `THRESHOLD_CHOICES`, `NOTIF_KEYS`, `notif_allowed(kind, config)` (already
wired to `emit_notif`, no further call-site change needed here).

<verification>
- `python3 claude-monitor.py --selfcheck` still exits 0 (no new `demo()` assertions this plan -- everything new here is GTK construction, proven live and via source-shape checks).
- The "Notifications" submenu (mute + 4 ordered event checkboxes) and the nested "Badge threshold" submenu (4 ordered radio presets) exist, wired into `rebuild_menu`.
- No `Gtk.Entry` (free-text) widget anywhere in the file.
- `notif_allowed`'s and `build_label`'s call sites are byte-identical to Plan 1's -- mute is not wired into any second path.
- Live UAT: toggling each event type takes effect on the very next event with no restart (SC1); mute silences all notifications while rows/badge keep working (SC2); mute-off restores prior per-event state (D-04); the threshold selection persists across a restart (SC3, this setting specifically) and the badge follows it (SC5).
- ASCII only: `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
</verification>

<success_criteria>
- Each of the four event types can be switched on/off from the tray menu; the next event honors the change with no restart (CFG-01, ROADMAP SC1).
- A single "Mute all" toggle silences every notification while tray rows and icon badge keep working (CFG-02, ROADMAP SC2).
- The badge threshold is chosen from four fixed presets, never free text, and the badge follows the selection (CFG-05, ROADMAP SC5, D-05).
- Turning mute off restores exactly the prior per-event toggle states, with no re-sync step (D-04).
</success_criteria>

<output>
Create `.planning/workstreams/notifications-predictive-alerts/phases/06-notification-control-config/06-02-SUMMARY.md` when done.
</output>
