---
phase: 10-tui-polish-btop-style
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - claude_monitor/core.py
  - claude_monitor/test_claude_monitor.py
  - claude-tui.py
autonomous: true
requirements: [TUI-08]
must_haves:
  truths:
    - "`just selfcheck` exits 0 on stock /usr/bin/python3 with core.spark_levels present (green gate; only possible if core imports no third-party package)."
    - "core.spark_levels round-trips all 8 SPARK_GLYPHS levels: spark_levels(SPARK_GLYPHS) == [0,1,2,3,4,5,6,7], and a SPARK_GAP column decodes to None."
    - "The column heights the graph draws are the decoded levels themselves -- no new trend math; the sparkline it decodes is build_trend_rows / trend_sparkline output reused verbatim (D-05/D-06)."
    - "The trends panel renders a taller, height-colored (green->red ramp) multi-row column graph, richer than the reused one-line tray sparkline (TUI-08)."
    - "Degraded/collecting state renders and does not crash: when trends is None or empty (D-06/D-07 carried from Phase 9), the panel still shows core.trend_text's 'trends: collecting history...' message and spark_levels is never called on it."
  artifacts:
    - "claude_monitor/core.py: spark_levels(sparkline)"
    - "claude_monitor/test_claude_monitor.py: new assert block for spark_levels under demo()"
    - "claude-tui.py: decoded multi-row colored column graph in render_all's #trends seam"
  key_links:
    - "render_all reads snap['trends'] (build_trend_rows output); trends[0] is the 24-char sparkline decoded by core.spark_levels; the today/wk/peak text rows (trends[1:]) render below unchanged."
    - "Height color reuses Plan 10-01's band -> ANSI-style ramp so the three visuals share one palette."
  prohibitions:
    - "claude_monitor/core.py must NOT import textual or rich (proven by --selfcheck on stock /usr/bin/python3)."
    - "No new trend math: spark_levels inverts the existing SPARK_GLYPHS mapping and reuses build_trend_rows / trend_sparkline output; it computes no new series and reads no new snapshot field (D-05/D-06)."
    - "No new data source, no new polling, no IPC/snapshot-shape change, no new runtime dependency."
    - "The trends graph renders as a rich.Text renderable, not a markup string (the #trends Static is markup=False)."
    - "The collecting-state message path (trends falsy -> core.trend_text) is preserved and never routed through spark_levels."
---

<objective>
Turn the trends panel from the one-line reused tray sparkline into a btop-style taller,
height-colored column graph (TUI-08) -- with no new trend data. The snapshot shape is
fixed (D-05), so the graph decodes the existing 24-level SPARK_GLYPHS sparkline that
build_trend_rows already delivers back to its levels (0-7) and draws each column that
many block-rows tall, colored along the same green->red ramp as the usage gauge. The
decode is a pure function in claude_monitor.core, proven by `--selfcheck`; claude-tui.py
draws the colored rows.

Purpose: TUI-08, richer trends graph sourced entirely from core's existing trend output.
Output: core.spark_levels, its asserts, and a taller colored trends graph.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/notifications-predictive-alerts/ROADMAP.md
@.planning/workstreams/notifications-predictive-alerts/STATE.md
@.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-CONTEXT.md

# The glyph mapping to invert + the trend row source (no new trend math)
@claude_monitor/core.py
# The --selfcheck assert suite spark_levels extends
@claude_monitor/test_claude_monitor.py
# render_all's #trends seam + CSS; the band -> ANSI ramp established in 10-01
@claude-tui.py

# Prior plan: the shared band -> ANSI-style palette this graph's height coloring reuses
@.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core.spark_levels -- decode the SPARK_GLYPHS sparkline back to levels</name>
  <files>claude_monitor/core.py, claude_monitor/test_claude_monitor.py</files>
  <read_first>
    - claude_monitor/core.py:243 (SPARK_GLYPHS = the 8-level block ramp), :244 (SPARK_GAP = the empty-hour space)
    - claude_monitor/core.py:439 (trend_sparkline) -- produces the 24-char string spark_levels inverts
    - claude_monitor/core.py:490 (build_trend_rows) -- rows[0] is that sparkline; the reused output, no new trend math
    - claude_monitor/test_claude_monitor.py:252-304 (trend logic asserts) and :735-749 (tui trend text asserts) -- where spark_levels' asserts sit
  </read_first>
  <behavior>
    - spark_levels(SPARK_GLYPHS) == [0, 1, 2, 3, 4, 5, 6, 7] (every glyph round-trips to its index)
    - spark_levels(SPARK_GAP) == [None] (an empty-hour column decodes to None, not a level)
    - spark_levels(a real trend_sparkline output) has length 24 and every element is an int 0-7 or None
    - an unrecognized character decodes to None (tolerant; never raises / never indexes past the ramp)
  </behavior>
  <action>
    Add a pure function spark_levels(sparkline) to claude_monitor/core.py that maps each
    character of the sparkline string to its SPARK_GLYPHS index (0-7), or None for
    SPARK_GAP and for any character not in SPARK_GLYPHS (tolerant of a malformed/hostile
    field value). It inverts the exact SPARK_GLYPHS mapping trend_sparkline used -- no new
    trend math, no new series, no new snapshot field (D-06). The decoded level IS the
    column height, so no separate height function is needed: an 8-row-tall graph draws a
    column with level L filled L rows from the bottom. Place it near trend_sparkline, above
    the textual boundary; core imports no third-party package. Add asserts to demo() under a
    new "# --- tui spark levels (TUI-08) ---" block: the full-ramp round-trip against
    SPARK_GLYPHS, the SPARK_GAP -> None case, an unknown-char -> None case, and a decode of
    a real trend_sparkline(records) output asserting length 24 and every element in 0..7 or
    None.
  </action>
  <verify>
    <automated>just selfcheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def spark_levels' claude_monitor/core.py` shows spark_levels defined in core.
    - `just selfcheck` exits 0 (spark_levels asserts pass; core still stock-python3 importable, so it gained no textual/rich import).
    - spark_levels' body reads only SPARK_GLYPHS / SPARK_GAP and the input string -- it calls no trend_* recompute (source read).
  </acceptance_criteria>
  <done>spark_levels + its asserts exist and `just selfcheck` exits 0.</done>
  <reversibility rating="reversible">A pure inverse-lookup helper; no migration, no contract change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: render the decoded, height-colored taller trends graph</name>
  <files>claude-tui.py</files>
  <read_first>
    - claude-tui.py:180-202 (render_all) -- the #trends seam: currently core.trend_text(snap.get("trends"))
    - claude-tui.py:79-86 (compose) -- the #trends Static is markup=False, so the graph must be a rich.Text renderable
    - claude-tui.py:50-66 (CSS block) -- #trends height:auto; where a taller graph's rows and any per-height style may be declared
    - claude_monitor/core.py:786 (trend_text) -- the collecting-state producer that must still render when trends is falsy (D-07)
    - claude_monitor/core.py band (Plan 10-01) -- reused to color each column by its height fraction along the green->red ramp
  </read_first>
  <action>
    In claude-tui.py render_all, replace the single-line trends render for the graph row
    only: when snap['trends'] is falsy, keep exactly today's behavior -- render
    core.trend_text(snap.get("trends")), which shows "trends: collecting history..." (D-07,
    never crash, spark_levels is not called). When trends is present, decode trends[0] (the
    24-char sparkline) with core.spark_levels and draw a taller multi-row block column graph
    as a single rich.Text renderable: an 8-row-tall band of columns where column i is filled
    from the bottom up to its decoded level (None columns stay blank, preserving the
    time-aligned gaps), using a block glyph, and each filled cell colored by core.band
    applied to its height fraction (band(level / 7 * 100)) so the graph shares the usage
    gauge's green->red palette. Render the remaining text rows trends[1:] (today/wk/hr,
    peak hour) below the graph unchanged -- they stay core's exact strings (D-05). Colors
    are ANSI style names, not RGB, for the ansi-dark inherited palette. The #trends Static
    is markup=False, so pass a Text renderable, never a markup string (same no-parse posture
    as the usage panel and the sessions cells).
  </action>
  <verify>
    <automated>just selfcheck</automated>
    <human-check>`just tui` with history built up: the trends panel shows a taller, colored column graph (green->red by column height) above the today/wk/peak text lines. With a fresh/empty history (or daemon just started) it shows "trends: collecting history..." and does not crash.</human-check>
  </verify>
  <acceptance_criteria>
    - render_all decodes trends[0] via core.spark_levels and builds a multi-row rich.Text graph colored by core.band; trends[1:] render below (source read of render_all).
    - The falsy-trends branch still calls core.trend_text and never calls spark_levels (source read).
    - `just selfcheck` exits 0 (core unaffected; still stock-python3 importable).
    - No new trend/number formatting is added to claude-tui.py: the text rows are core.trend_text / build_trend_rows output, not re-formatted in-file.
  </acceptance_criteria>
  <done>The trends panel renders a taller height-colored graph from decoded levels, the collecting-state message still renders on empty trends, and `just selfcheck` exits 0.</done>
  <reversibility rating="reversible">Local render change in the only textual file; no migration, no contract change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon unix socket -> claude-tui.py | The same read-only snapshot verb (0600, local); the `trends` field (a list of strings, or None) reaches the new decode + graph render. No new boundary this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | core.spark_levels over a malformed trends[0] | medium | mitigate | spark_levels maps any character not in SPARK_GLYPHS (including unexpected glyph bytes) to None rather than indexing past the ramp or raising; asserted for the unknown-char case. Height derivation uses the decoded level, so an out-of-set byte yields a blank column, not an index error. |
| T-10-04 | Denial of Service | render_all trends graph on None/empty or malformed trends | medium | mitigate | The falsy-trends branch is preserved (core.trend_text renders the collecting message, spark_levels never called); a render-time raise on a malformed present-trends value is caught by tick()'s except (claude-tui.py:170-178) and surfaced as "render error", not mislabelled "daemon unreachable". |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package install this plan: textual stays the only third-party dep, already lock-pinned (T-09-SC in force); spark_levels is stdlib-only core. No legitimacy gate required. |
</threat_model>

<verification>
- `just selfcheck` exits 0 (spark_levels asserts pass on stock /usr/bin/python3).
- `just lint` clean.
- Live TUI: trends panel shows a taller colored column graph; empty history shows the collecting message.
</verification>

<success_criteria>
- TUI-08: the trends panel renders a richer (taller + height-colored) usage graph than the reused tray sparkline, sourced from core's existing trend output with no new trend math.
- core.spark_levels is pure, round-trips all 8 SPARK_GLYPHS levels, tolerates malformed input, and never imports textual/rich.
- The collecting-state message still renders when trends is None/empty.
</success_criteria>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift/source-grounding verification):
- `claude_monitor/core.py`: `spark_levels(sparkline)`
- `claude_monitor/test_claude_monitor.py`: new assert lines under demo() in block "# --- tui spark levels (TUI-08) ---" (no new function symbol)
- `claude-tui.py`: the decoded multi-row colored column-graph rendering inside render_all's #trends branch (a small render helper and/or a graph-height module constant may be introduced; render-only, not a formatter)
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-02-SUMMARY.md` when done
</output>
