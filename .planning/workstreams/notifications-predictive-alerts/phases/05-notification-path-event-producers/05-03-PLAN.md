---
phase: 05-notification-path-event-producers
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - claude-monitor.py
autonomous: true
requirements: [ALERT-02, ALERT-03, ALERT-04, NOTIF-02]

must_haves:
  truths:
    - "When either cap (5-hour or 7-day) is projected to hit 100% before its window resets, a notification fires once (ROADMAP SC3, ALERT-02/03)."
    - "When the projection says usage coasts to reset, nothing fires (ROADMAP SC3)."
    - "A cap that has already alerted stays silent for the rest of that window even if the projection climbs further (D-06)."
    - "After a cap's window rolls over -- a new `resets_at_epoch` -- that cap can alert again (ROADMAP SC4, ALERT-04)."
    - "An already-exhausted cap (exhaust time in the past) stays silent with no special case (D-07)."
    - "The 7-day block being absent on an older CLI degrades to silence, not a crash."
    - "Clicking a quota notification opens the usage dashboard (D-09)."
    - "`python3 claude-monitor.py --selfcheck` exits 0 with the projection port and the arm/re-arm state machine asserted."
  artifacts:
    - "claude-monitor.py: WIN5, WIN7, ALERT_LEAD module constants"
    - "claude-monitor.py: project(pct, reset, win, now) -- Python port of the QUOTA-03 JS projection"
    - "claude-monitor.py: hhmm(epoch) -- local wall-clock formatter"
    - "claude-monitor.py: alert_due(p, now) -- the D-05 lead-time predicate"
    - "claude-monitor.py: alert_should_fire(armed_reset, reset, p, now) -- the D-06 arm/re-arm predicate"
    - "claude-monitor.py: Monitor.alert_armed -- per-cap armed-window dict"
    - "claude-monitor.py: the ALERT emit inside poll_loop's per-tick body"
    - "claude-monitor.py: demo() assertions for project / alert_due / alert_should_fire"
  key_links:
    - "The operative D-05 predicate is `\"exhaust\" in p and p[\"exhaust\"] - now >= ALERT_LEAD`. Testing `proj >= 100` first and then reading `p[\"exhaust\"]` raises KeyError at exactly proj == 100.0, because the projection sets `exhaust` only when proj > 100 STRICTLY."
    - "`alert_armed[cap]` stores the `resets_at_epoch` of the window in which that cap last fired. A changed reset epoch IS a new window, definitionally -- that is ALERT-04's re-arm, with no timer and no clock math."
    - "The ALERT producer runs inside `poll_loop`'s existing per-tick body, which already has the blanket except + traceback from `260713-fry`. No new polling, no new data source."
    - "The JavaScript project() at :931 STAYS. It recomputes against a live browser clock as the static page ages; the Python port runs on the poll tick. Same arithmetic, two clocks."
---

<objective>
Ride the shared path with the second producer: predictive 5-hour and 7-day quota alerts,
evaluated on the existing poll tick.

Carries the Python port of the QUOTA-03 `project()` formula, which today exists only as
JavaScript inside the dashboard HTML -- `poll_loop` computes no projection at all. The port
is mechanical arithmetic with settled semantics, not a new forecaster.

Purpose: ALERT-02 / ALERT-03 / ALERT-04, and the proof that `emit_notif` generalizes past
one producer -- the phase's actual thesis.
Output: four pure functions, a per-cap arm dict, and one emit inside `poll_loop`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-notification-path-event-producers/05-RESEARCH.md
@.planning/phases/05-notification-path-event-producers/05-CONTEXT.md
@.planning/phases/05-notification-path-event-producers/05-01-SUMMARY.md
@.planning/phases/05-notification-path-event-producers/05-02-SUMMARY.md
@claude-monitor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port project() / hhmm() to Python and add the two alert predicates</name>

  <read_first>
    - `claude-monitor.py:924-948` -- the JavaScript `fmtDur`, `hhmm` (:930) and `project` (:931-948) inside the dashboard HTML string. This is the source of truth being transcribed. Note `if(e<=0.05)return {early:true}`, `if(e>1)e=1`, and that `exhaust` is set only when `out.proj>100` **strictly** and `pct>0`.
    - `claude-monitor.py:746` -- `var WIN5=18000,WIN7=604800` in the JS. The Python constants must carry the same literals.
    - `claude-monitor.py:508-509` -- `_is_num(v)`, the existing numeric guard the port needs.
    - `.planning/phases/05-notification-path-event-producers/05-RESEARCH.md` -- "Secondary Question 2: the `project()` port", **in full**: the transcribed function, the edge-case table, the D-05 predicate subtlety, and the 10 executed test cases. Also "Secondary Question 4" -> "ALERT arm/re-arm (D-06, ALERT-04)". Also "Common Pitfalls" #7.
    - `.planning/phases/05-notification-path-event-producers/05-CONTEXT.md` -- D-05, D-06, D-07, D-08.
  </read_first>

  <files>claude-monitor.py</files>

  <behavior>
    With `R = 1_000_000`, `S = R - WIN5`:
    - `project(None, R, WIN5, S + 9000)` -> `None`; same for `project(50.0, None, ...)` and `project("x", R, ...)` (non-numeric degrades to None, not TypeError -- `seven_day_*` is exactly this shape on an older CLI).
    - `project(50.0, R, WIN5, S + 900)` -> `{"early": True}` (e == 0.05 exactly).
    - `project(50.0, R, WIN5, S + 901)` -> has a `"proj"` key (e just over the guard).
    - `project(50.0, R, WIN5, S - 5000)` -> `{"early": True}` (negative e from clock skew).
    - `project(50.0, R, WIN5, S + WIN5 // 2)` -> `proj == 100.0` exactly AND **no** `"exhaust"` key.
    - `project(60.0, R, WIN5, S + WIN5 // 2)` -> `proj == 120.0`, `exhaust == S + 15000.0`, `exhaust < R`.
    - `project(10.0, R, WIN5, S + WIN5 // 2)` -> `proj == 20.0` (coasting).
    - `project(42.0, R, WIN5, R + 3600)` -> `proj == 42.0`, no `"exhaust"` (window over -> e clamped to 1).
    - `project(0.0, R, WIN5, S + WIN5 // 2)` -> `proj == 0.0` (no div-by-zero).
    - `project(80.0, R7, WIN7, S7 + WIN7 // 2)` -> `proj == 160.0` (same fn, 7d window).
    - `alert_due({"proj": 200.0, "exhaust": now + 60}, now)` -> False (< 15 min lead, D-05).
    - `alert_due({"proj": 200.0, "exhaust": now + 901}, now)` -> True.
    - `alert_due({"early": True}, now)` -> False; `alert_due(None, now)` -> False.
    - `alert_should_fire(None, R, hot, now)` -> True (never armed + projected over).
    - `alert_should_fire(R, R, hot, now)` -> False (already fired in THIS window, D-06).
    - `alert_should_fire(R, R + WIN5, hot, now)` -> True (window rolled -> re-arm, ALERT-04).
    - `alert_should_fire(None, R, cold, now)` -> False (coasting).
    - `alert_should_fire(None, None, None, now)` -> False (7d cap absent on an older CLI).
    - D-07: `dead = project(200.0, R, WIN5, R + WIN5 // 2)` has an `exhaust` in the PAST, so `alert_should_fire(None, R, dead, R + WIN5 // 2)` -> False. No special case, no code.
  </behavior>

  <action>
    Add three module constants near the other constants: `WIN5 = 18000` (5 hours in seconds),
    `WIN7 = 604800` (7 days), and `ALERT_LEAD = 15 * 60` (D-05's lead time -- a projection that
    says "you run out in 90 seconds" is not actionable, and the existing >80% badge already
    covers "you're nearly there"). `WIN5` / `WIN7` must carry the same literals the dashboard
    JS uses at :746.

    Add module-level `project(pct, reset, win, now)` -- a verbatim transcription of the JS at
    :931, with `now` passed in as an argument instead of read from the clock (so it is pure and
    assertable). Semantics preserved exactly:
    - Return `None` when `pct` or `reset` fails `_is_num` (:508). **This is the one place the
      Python port must be STRICTER than the JS**: JS coerces, Python would raise `TypeError`.
      `seven_day_pct` / `seven_day_reset` come back `None` on an older CLI (`parse_usage` lets
      them through unguarded, unlike `used_percentage` / `resets_at_epoch`), so this guard is
      what makes the 7d alert degrade silently -- exactly as `usage_rows` already does.
    - `start = reset - win`, `e = (now - start) / float(win)`.
    - `e <= 0.05` -> return `{"early": True}`. This is D-05's noisy-start suppressor, and it
      doubles as the clock-skew guard: a negative `e` (a reset epoch further out than one whole
      window) also lands here.
    - `e > 1` -> clamp to `1.0`, so the projection degrades to the current pct for a window
      that is already over. No crash.
    - `out = {"proj": pct / e}`; then, only when `out["proj"] > 100` **and** `pct > 0`, compute
      `exh = start + (100.0 / pct) * (now - start)` and set `out["exhaust"] = exh` only when
      `exh < reset`. The `pct > 0` clause is what guards the `100.0 / pct` division.

    Docstring the port with the **deliberate duplication**: the JavaScript copy at :931 stays.
    It recomputes against a live browser clock as the static dashboard page ages between
    regenerations, so it cannot be replaced by a value baked in from Python. Same arithmetic,
    two clocks. If one changes, both change, and `demo()`'s assertions pin both. Mark it with a
    `ponytail:` comment naming that ceiling and the upgrade path (collapse only if the page ever
    stops recomputing live).

    Add module-level `hhmm(epoch)` returning `time.strftime("%H:%M", time.localtime(epoch))` --
    the stdlib one-liner, and it is right about DST. D-08 wants a local wall-clock time, not a
    duration. (`time` is already imported at :22.)

    Add module-level `alert_due(p, now)`, returning
    `bool(p) and "exhaust" in p and (p["exhaust"] - now) >= ALERT_LEAD`.

    That single expression carries D-05, D-07, and the early-guard suppression at once, and it
    is written this way on purpose: `exhaust` exists only when `proj > 100` **strictly**, so a
    predicate that tests `proj >= 100` first and then reads `p["exhaust"]` raises `KeyError` at
    exactly `proj == 100.0`. The membership test subsumes the projection test -- there is no
    `proj` clause to write. `{"early": True}` has no `exhaust` key either, so it is silent for
    free, and an already-exhausted cap has an `exhaust` in the past, so D-07's "no special case,
    no code" claim holds by construction. Say all of this in the docstring; it is the single
    subtlest thing in the phase.

    Add module-level `alert_should_fire(armed_reset, reset, p, now)`:
    - Return `False` when `reset` fails `_is_num` (the 7d cap is absent on an older CLI).
    - Return `False` when `armed_reset == reset` -- this cap already alerted in THIS window
      (D-06). It stays silent for the remainder of the window even if the projection climbs
      further.
    - Otherwise return `alert_due(p, now)`.

    Docstring the ALERT-04 mechanism, because it is the reason there is no timer here:
    `armed_reset` stores the `resets_at_epoch` of the window in which that cap last fired. A
    changed reset epoch **is** a new window, definitionally -- so `armed_reset != reset` re-arms
    the cap for free, with no reset-detection code and no clock math. There is deliberately no
    re-arm on the projection dipping under 100 and climbing back inside the same window; that
    flaps around the boundary (D-06). Lead-time step re-fires ("~30m left") are deferred.

    Triple-quoted docstrings throughout. ASCII only -- including the `--` in any prose.
  </action>

  <acceptance_criteria>
    - Four new module-level functions with these exact signatures: `python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
f={x.name:[a.arg for a in x.args.args] for x in t.body if isinstance(x,ast.FunctionDef)}
want={'project':['pct','reset','win','now'],'hhmm':['epoch'],'alert_due':['p','now'],'alert_should_fire':['armed_reset','reset','p','now']}
sys.exit(0 if all(f.get(k)==v for k,v in want.items()) else 1)"` exits `0`.
    - `grep -cE '^(WIN5|WIN7|ALERT_LEAD) =' claude-monitor.py` returns `3`, and `grep -c 'WIN5 = 18000' claude-monitor.py` returns `1` and `grep -c 'WIN7 = 604800' claude-monitor.py` returns `1` (the JS literals at :746 are matched).
    - `alert_due` does not test the projection value: `python3 -c "import ast,sys; t=ast.parse(open('claude-monitor.py').read()); f=[x for x in t.body if isinstance(x,ast.FunctionDef) and x.name=='alert_due'][0]; sys.exit(1 if \"'proj'\" in ast.dump(f) or '\"proj\"' in ast.dump(f) else 0)"` exits `0` -- the KeyError trap (Pitfall 7) is structurally impossible.
    - The JavaScript `project` is untouched: `grep -c 'function project(pct,reset,win)' claude-monitor.py` still returns `1`.
    - `project` is pure -- it reads no clock: `python3 -c "import ast,sys; t=ast.parse(open('claude-monitor.py').read()); f=[x for x in t.body if isinstance(x,ast.FunctionDef) and x.name=='project'][0]; sys.exit(1 if 'time' in ast.dump(f) else 0)"` exits `0`.
    - `python3 claude-monitor.py --selfcheck` exits `0` and prints `ok` (this task adds the functions; Task 2 adds their assertions -- both must pass).
    - `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>python3 claude-monitor.py --selfcheck</automated>
  </verify>

  <done>`WIN5` / `WIN7` / `ALERT_LEAD`, plus pure `project()`, `hhmm()`, `alert_due()` and `alert_should_fire()` exist at module level. The JS copy at :931 is unchanged. `--selfcheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the port and the arm/re-arm state machine in --selfcheck</name>

  <read_first>
    - `claude-monitor.py:1033` and the tail of `demo()` around `:1415-1430` -- the `assert`-based self-check and its closing `print("ok")`. New assertions go before that print, after Plan 02's `sess_should_notify` block.
    - `.planning/phases/05-notification-path-event-producers/05-RESEARCH.md` -- "Secondary Question 2" -> the "Test cases (all 10 executed and passing against the port)" block, and "Secondary Question 4" -> the "Assertion cases for `--selfcheck`" block (the ALERT arm/re-arm cases). Both are ready to transcribe.
    - `.planning/phases/05-notification-path-event-producers/05-RESEARCH.md` -- "Validation Architecture" -> the "Phase Requirements -> Test Map" table.
  </read_first>

  <files>claude-monitor.py</files>

  <action>
    Add the projection-port and arm/re-arm assertions to `demo()`, before the closing
    `print("ok")` at :1430. Transcribe every case from RESEARCH.md's two assertion blocks --
    all 10 `project()` cases and all of the `alert_due` / `alert_should_fire` cases listed in
    this plan's Task 1 `<behavior>` block. Group them under two comments naming the
    requirements they pin: `ALERT-02/03 + D-05` for the projection and lead-time cases,
    `ALERT-04 + D-06 + D-07` for the arm/re-arm cases.

    Use `abs(x - y) < 1e-9` for float comparisons, never `==` on a division result -- except
    where the case is specifically asserting an exact value the arithmetic must produce
    (`proj == 100.0` at half window with 50% used; `proj == 0.0` at pct 0). Use synthetic
    epochs (`R = 1_000_000`, `S = R - WIN5`), never `time.time()` -- these assertions must be
    deterministic and must not go stale.

    Four cases carry disproportionate weight; make sure each is present and commented:
    - `proj == 100.0` exactly at half-window with 50% used, AND `"exhaust" not in p`. This pins
      the strict-greater-than boundary that Pitfall 7's `KeyError` lives on.
    - A negative elapsed fraction (a reset epoch more than one window out -- clock skew) returns
      `{"early": True}`, not a garbage projection.
    - `alert_should_fire(R, R + WIN5, hot, now)` is True -- the window rolled, the cap re-arms
      (ALERT-04 / ROADMAP SC4). This is the only assertion that pins the re-arm.
    - `dead = project(200.0, R, WIN5, R + WIN5 // 2)` (expired AND over 100, so `exhaust` lands
      in the PAST) -> `alert_should_fire(None, R, dead, R + WIN5 // 2)` is False. This asserts
      D-07's "no special case, no code" claim, and it is the only thing that would catch it if
      someone later "helpfully" adds one.

    Also assert `hhmm` is stable and ASCII: `hhmm(0)` returns a 5-character `HH:MM` string
    (assert on its length and that it contains `":"`; do not assert a specific value -- it is
    timezone-dependent by design, D-08 wants local wall-clock).
  </action>

  <acceptance_criteria>
    - `python3 claude-monitor.py --selfcheck` exits `0` and prints `ok`.
    - Coverage: `grep -c 'assert.*project(' claude-monitor.py` returns at least `10`; `grep -c 'assert.*alert_should_fire(' claude-monitor.py` returns at least `7`; `grep -c 'assert.*alert_due(' claude-monitor.py` returns at least `2`.
    - Determinism: `python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
d=[x for x in t.body if isinstance(x,ast.FunctionDef) and x.name=='demo'][0]
new=[n for n in ast.walk(d) if isinstance(n,ast.Assert) and ('alert_should_fire' in ast.dump(n) or 'alert_due' in ast.dump(n))]
sys.exit(1 if any('time' in ast.dump(n) for n in new) else 0)"` exits `0` -- no wall clock in the alert assertions.
    - Mutation check (the assertions are load-bearing, not decorative). Each of these edits, applied alone, MUST make `--selfcheck` exit non-zero:
      (a) change the early guard from `e <= 0.05` to `e <= 0`;
      (b) change the exhaust condition from `out["proj"] > 100` to `out["proj"] >= 100`;
      (c) change `ALERT_LEAD` from `15 * 60` to `0`;
      (d) delete the `armed_reset == reset` clause from `alert_should_fire`.
      Revert each after checking.
    - `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>python3 claude-monitor.py --selfcheck</automated>
  </verify>

  <done>`demo()` pins all 10 projection cases, the lead-time predicate, the arm/re-arm state machine, and D-07's no-special-case claim. The four mutation checks each fail `--selfcheck`.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the ALERT producer into poll_loop</name>

  <read_first>
    - `claude-monitor.py:1726-1772` -- `poll_loop` in full. The per-tick body already computes `usage = fetch_usage()` and `now = time.time()` at :1751-1752 and is already wrapped in the blanket `except Exception: traceback.print_exc()` from quick task `260713-fry`. The ALERT producer goes inside that existing body -- it inherits the guard, adds no polling, and adds no data source.
    - `claude-monitor.py:1433-1439` -- `Monitor.__init__`'s state block, where `alert_armed` joins `notif_slots` / `notif_acts` from Plan 01.
    - `claude-monitor.py:1468-1469` -- `Monitor.open_dashboard`, the click action (D-09). Zero-I/O `webbrowser.open` of the pre-written `file://`.
    - `.planning/phases/05-notification-path-event-producers/05-RESEARCH.md` -- "Secondary Question 4" -> the "Caller (in `poll_loop`, on fire...)" code block; "Q6" (async emit is safe from the poll thread); "Code Examples" -> the ALERT call site.
    - `.planning/phases/05-notification-path-event-producers/05-CONTEXT.md` -- D-05, D-06, D-08, D-09, and the quota-alert popup shape under "Specific Ideas".
    - `05-01-SUMMARY.md` -- `Monitor.emit_notif`'s exact argument order and the `("dash",)` action-tuple shape handled by `on_notif_signal`.
  </read_first>

  <files>claude-monitor.py</files>

  <action>
    Add `self.alert_armed = {}` to `Monitor.__init__` alongside `notif_slots` / `notif_acts`.
    It maps a cap key (`"5h"` / `"7d"`) to the `resets_at_epoch` of the window in which that cap
    last alerted. In-memory only, correctly lost on restart -- a restart legitimately re-arms
    every cap. No new file, no persistence (that is Phase 6).

    Inside `poll_loop`'s existing per-tick `try` body, after the `if usage is not None:
    append_history(...)` at :1753-1754 and before the `GLib.idle_add(mon.apply_usage, usage)`
    at :1766, add the alert evaluation. Guard the whole block on `usage is not None`.

    Iterate both caps in one loop over tuples of `(cap_key, pct, reset, win, title)`:
    - `("5h", usage["used_percentage"], usage["resets_at_epoch"], WIN5, "5-hour quota")`
    - `("7d", usage["seven_day_pct"], usage["seven_day_reset"], WIN7, "7-day quota")`

    Read the 7d fields with `.get(...)` -- they are `None` on an older CLI, and `project()`'s
    `_is_num` guard plus `alert_should_fire`'s own `_is_num(reset)` check turn that into silence
    rather than a crash. Do not add a second, redundant guard.

    Per cap: compute `p = project(pct, reset, win, now)` with the `now` already read at :1752,
    then gate on `alert_should_fire(mon.alert_armed.get(cap), reset, p, now)`. When it fires:
    - Call `mon.emit_notif(...)` with `key` = `("cap", cap)` -- one notification slot per cap
      (D-03), so a later alert for the same cap overwrites its popup in place.
    - `kind` = `cap`, i.e. the string `"5h"` or `"7d"` -- the other two of the four event-type
      keys `notif_allowed` gates on (NOTIF-02 seam, Phase 6).
    - `title` = the cap's `title` string, `"5-hour quota"` or `"7-day quota"` (D-08).
    - `body` = `"Projected %d%% at reset -- runs out ~%s" % (round(p["proj"]), hhmm(p["exhaust"]))`.
      D-08: the body carries both the severity and the actionable clock time, and the exhaust
      time is a local wall-clock time, not a duration. `round()` on the projection -- a
      "Projected 140.237% " popup helps nobody. Both interpolated values are numbers we computed
      ourselves (a rounded float and a `strftime` output), so no user-controlled string reaches
      the markup-parsed body (threat T-05-07). ASCII `--`, not an em-dash.
    - `action` = `("dash",)` -- D-09. `Monitor.on_notif_signal` dispatches this to the existing
      `Monitor.open_dashboard` (:1468). The dashboard is exactly where you would go to inspect
      the projection you were just warned about.
    - `urgency` = `URGENCY_NORMAL`. A quota alert is informational; it does not need to block
      the screen the way an unanswered permission prompt does.
    - Then, and only then, `mon.alert_armed[cap] = reset` -- arm it, silent until this cap's
      reset epoch changes (D-06, ALERT-04).

    Both `p["proj"]` and `p["exhaust"]` are safe to read here **because** `alert_should_fire`
    returned True, which required `"exhaust" in p` -- and `exhaust` only exists when `proj > 100`.
    Do not add a defensive `.get()`; it would only hide a broken predicate.

    `emit_notif` is async and safe to call from this daemon thread (its reply callback is
    dispatched onto the Gtk main thread by the proxy's construction-time main context), so this
    adds nothing blocking to `poll_loop` and nothing at all to the Gtk main loop. No `idle_add`
    is needed around it.

    Do not add a new poll, a new interval, a new thread, or a second data source. Every value
    the alert needs is already in the `usage` dict that `parse_usage` produces on the existing
    tick.
  </action>

  <acceptance_criteria>
    - `Monitor.alert_armed` is initialized in `__init__`: `python3 -c "import ast,sys; t=ast.parse(open('claude-monitor.py').read()); c=[x for x in ast.walk(t) if isinstance(x,ast.ClassDef) and x.name=='Monitor'][0]; i=[x for x in c.body if isinstance(x,ast.FunctionDef) and x.name=='__init__'][0]; sys.exit(0 if 'alert_armed' in ast.dump(i) else 1)"` exits `0`.
    - The alert lives inside `poll_loop` and nowhere else: `python3 -c "import ast,sys; t=ast.parse(open('claude-monitor.py').read()); f=[x for x in t.body if isinstance(x,ast.FunctionDef) and x.name=='poll_loop'][0]; d=ast.dump(f); sys.exit(0 if 'alert_should_fire' in d and 'emit_notif' in d and 'project' in d else 1)"` exits `0`.
    - The emit is inside the existing guarded `try` body, not outside it: `python3 -c "
import ast,sys
t=ast.parse(open('claude-monitor.py').read())
f=[x for x in t.body if isinstance(x,ast.FunctionDef) and x.name=='poll_loop'][0]
w=[x for x in f.body if isinstance(x,ast.While)][0]
tr=[x for x in w.body if isinstance(x,ast.Try)][0]
sys.exit(0 if 'alert_should_fire' in ast.dump(tr) else 1)"` exits `0`.
    - No new polling: `grep -c 'threading.Thread' claude-monitor.py` still returns `3` (serve, poll_loop, watch_focus) and `grep -c 'time.sleep' claude-monitor.py` is unchanged from before this plan.
    - Both cap titles and the D-08 body template are present: `grep -c '"5-hour quota"' claude-monitor.py` returns `1`, `grep -c '"7-day quota"' claude-monitor.py` returns `1`, and `grep -c 'Projected %d%% at reset -- runs out ~%s' claude-monitor.py` returns `1`.
    - The dashboard click action is wired: `grep -c '("dash",)' claude-monitor.py` returns `1`.
    - `python3 claude-monitor.py --selfcheck` exits `0` and prints `ok`.
    - Behavior (ROADMAP SC3 / SC4) -- drive the predicates rather than waiting for real quota. With the helper importable in a REPL harness (or a scratch script that reuses the module's functions), confirm: given a synthetic usage dict projecting 140% with 40 minutes of lead, `alert_should_fire` returns True on the first tick and False on the second (armed); after replacing `resets_at_epoch` with a later epoch, it returns True again. On the live tray, at least one real alert popup must be observed titled `5-hour quota` with a body of the form `Projected NNN% at reset -- runs out ~HH:MM`, and clicking it must open the dashboard in the browser.
    - `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>python3 claude-monitor.py --selfcheck</automated>
    <human-check>Live tray: a quota alert popup appears titled "5-hour quota" with the projected percentage and a local HH:MM exhaust time; clicking it opens the dashboard; the same cap does not re-alert on subsequent poll ticks within the same window.</human-check>
  </verify>

  <done>`poll_loop`'s existing per-tick body evaluates both caps through `project()` + `alert_should_fire()`, emits at most one notification per cap per window through the shared `Monitor.emit_notif`, and arms the cap on its `resets_at_epoch`. No new thread, no new poll, no new data source. `--selfcheck` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `claude-monitor` CLI stdout -> `parse_usage` -> `project()` -> notification body | The usage payload is JSON from a locally-installed CLI. Its numeric fields reach the markup-parsed notification body -- but only after passing through `_is_num`, `round()`, and `strftime`. No string from the payload is interpolated. |
| session D-Bus -> `on_notif_signal` -> `Monitor.open_dashboard` | This plan supplies the second action tuple (`("dash",)`). The broadcast filter is Plan 01's T-05-01. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | quota-alert notification body | medium | mitigate | The daemon parses the notification body as Pango markup (`useBodyMarkup: true`). The alert body interpolates exactly two values: `round(p["proj"])` -- an integer from our own arithmetic -- and `hhmm(p["exhaust"])` -- a `time.strftime("%H:%M", ...)` output. Neither can carry a markup character; no string from the CLI payload reaches the body. Mitigation is structural: **do not interpolate any payload-derived string into the body.** If a future phase needs one, it must go through `GLib.markup_escape_text()` or move to the summary (which this daemon does not markup-parse). |
| T-05-08 | Elevation of Privilege | `("dash",)` action tuple | low | mitigate | Dispatched only when `on_notif_signal` finds the daemon-issued id in `self.notif_acts` (Plan 01, T-05-01). The action itself is `Monitor.open_dashboard`, which does zero I/O and opens a fixed `file://` path built with `pathlib.Path(DASH_PATH).resolve().as_uri()` -- no attacker-supplied component. |
| T-05-09 | Denial of Service | `project()` division | low | mitigate | Three divisions, three structural guards: `/ win` (a nonzero module constant), `pct / e` (unreachable when `e <= 0.05`), `100.0 / pct` (guarded by `pct > 0`). A malformed or hostile usage payload -- including a clock-skewed reset epoch producing a negative elapsed fraction -- degrades to `None` or `{"early": True}`, never a raise. Asserted in `--selfcheck` (Task 2). `poll_loop`'s blanket `except` is the backstop, not the control. |
| T-05-SC | Tampering | supply chain | n/a | accept | No package installs in this phase. |
</threat_model>

## Artifacts this phase produces

New symbols created by **this plan** in `claude-monitor.py`:

| Symbol | Kind | Notes |
|--------|------|-------|
| `WIN5` | module constant | `18000` -- 5h window, matches the JS literal at :746 |
| `WIN7` | module constant | `604800` -- 7d window, matches the JS literal at :746 |
| `ALERT_LEAD` | module constant | `15 * 60` -- D-05's minimum actionable lead time |
| `project(pct, reset, win, now)` | module function | Python port of the QUOTA-03 JS projection at :931 (the JS copy STAYS) |
| `hhmm(epoch)` | module function | `time.strftime("%H:%M", time.localtime(epoch))` -- D-08 |
| `alert_due(p, now)` | module function | the D-05 / D-07 lead-time predicate |
| `alert_should_fire(armed_reset, reset, p, now)` | module function | the D-06 / ALERT-04 arm/re-arm predicate |
| `Monitor.alert_armed` | instance attribute | dict: cap key -> `resets_at_epoch` of the window it last alerted in |
| cap keys `"5h"`, `"7d"` | dict keys | also the `kind` values `notif_allowed` gates on |
| title literals `"5-hour quota"`, `"7-day quota"` | string literals | D-08 |

Consumed from Plan 01 (already created there, not new here): `Monitor.emit_notif`,
`URGENCY_NORMAL`, the `("cap", cap)` slot key, the `("dash",)` action tuple.
Consumed from the existing codebase: `_is_num` (:508), `Monitor.open_dashboard` (:1468),
`poll_loop`'s per-tick body (:1749).

Sibling plans add: the notification path (Plan 01); `sess_should_notify` (Plan 02).

<verification>
- `python3 claude-monitor.py --selfcheck` exits 0 with all 10 projection cases plus the arm/re-arm cases pinned.
- All four mutation checks in Task 2 make `--selfcheck` fail.
- `alert_due` tests `"exhaust" in p`, never `proj >= 100` (Pitfall 7 -- the `KeyError` at exactly proj == 100.0 is structurally impossible).
- The ALERT producer lives inside `poll_loop`'s existing guarded per-tick body. No new thread, no new poll interval, no new data source.
- The JavaScript `project()` at :931 is unchanged; the duplication is documented where the Python port lands.
- The alert body interpolates only a rounded integer and a `strftime` output -- no payload string reaches the markup-parsed body (T-05-07).
- `("cap", cap)` slot key + `URGENCY_NORMAL` + `("dash",)` action.
- Live UAT: a real alert fires once per window, names the cap and the local exhaust time, and opens the dashboard on click.
- ASCII only: `LC_ALL=C grep -nP '[^\x00-\x7F]' claude-monitor.py` returns nothing.
</verification>

<success_criteria>
- Either cap projected past 100% before its reset (with >= 15 min lead) fires exactly one notification (ALERT-02/03, ROADMAP SC3).
- A coasting projection fires nothing (ROADMAP SC3).
- A rolled-over window re-arms the cap (ALERT-04, ROADMAP SC4).
- An already-exhausted cap stays silent with no special case (D-07).
- A missing 7-day block degrades to silence.
- Clicking a quota alert opens the dashboard (D-09).
- `--selfcheck` exits 0 and its assertions fail under mutation.
</success_criteria>

<output>
Create `.planning/phases/05-notification-path-event-producers/05-03-SUMMARY.md` when done.
</output>
