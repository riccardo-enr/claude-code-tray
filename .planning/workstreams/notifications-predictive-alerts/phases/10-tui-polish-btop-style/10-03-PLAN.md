---
phase: 10-tui-polish-btop-style
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - claude_monitor/core.py
  - claude_monitor/test_claude_monitor.py
  - claude-tui.py
autonomous: true
requirements: [TUI-09, TUI-10]
must_haves:
  truths:
    - "`just selfcheck` exits 0 on stock /usr/bin/python3 with core.sess_status_band present (green gate; only possible if core imports no third-party package)."
    - "core.sess_status_band maps the fixed status palette (D-07): waiting -> yellow, running -> green, done -> dim, and any unrecognized status -> a neutral default, asserted in --selfcheck."
    - "The live sessions table renders status-colored rows (waiting yellow / running green / done dim) with zebra striping, over the current plain DataTable (TUI-09)."
    - "Each of the three panels (usage, trends, sessions) is a titled, rounded, bordered box; borders are static, NOT threshold-tinted (TUI-10, D-08)."
  artifacts:
    - "claude_monitor/core.py: sess_status_band(status)"
    - "claude_monitor/test_claude_monitor.py: new assert block for sess_status_band under demo()"
    - "claude-tui.py: per-row status coloring + zebra_stripes on the DataTable, and rounded titled border CSS + border_title on all three panels"
  key_links:
    - "render_all's sess_rows loop styles each row's cells by core.sess_status_band(status) while every cell stays a rich.Text (the T-09-01 markup/control-char mitigation)."
    - "Border coloring reuses the ansi-dark palette (static color, no band coupling); titles come from textual border_title on each panel."
  prohibitions:
    - "claude_monitor/core.py must NOT import textual or rich (proven by --selfcheck on stock /usr/bin/python3)."
    - "The Text(...)-per-cell markup mitigation (T-09-01, claude-tui.py:199) MUST survive the restyle: every DataTable cell stays a rich.Text, never a bare str; status color is applied via Text style, never a markup string."
    - "Panel borders are static (no threshold tint on the chrome, D-08): the threshold signal lives in the row text (10-01) and gauge fill, not in the border color."
    - "No new number/string formatter is introduced in claude-tui.py: core stays the single source of every displayed value (D-05)."
    - "No new data source, no new polling, no IPC/snapshot-shape change, no new runtime dependency."
---

<objective>
Finish the btop look: status-color the live sessions table with zebra striping (TUI-09),
and wrap all three panels (usage, trends, sessions) in titled, rounded, bordered boxes
(TUI-10). The one assertable bit -- the fixed status -> color-name mapping -- lives in
claude_monitor.core, proven by `--selfcheck`; the coloring, striping, borders and titles
are CSS/render in claude-tui.py. Borders are static chrome (D-08); the threshold signal
stays in the row text and gauges from Plan 10-01.

Purpose: TUI-09 + TUI-10, the paneled, styled-sessions finish of the btop dashboard.
Output: core.sess_status_band, its asserts, colored+striped sessions, and titled rounded panels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/notifications-predictive-alerts/ROADMAP.md
@.planning/workstreams/notifications-predictive-alerts/STATE.md
@.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-CONTEXT.md

# sess_rows / SESS_RANK / _safe_cell -- the session cells being styled (single source, D-05)
@claude_monitor/core.py
# The --selfcheck assert suite sess_status_band extends
@claude_monitor/test_claude_monitor.py
# render_all's sessions loop + Text-per-cell mitigation (:199) + CSS block + compose panels
@claude-tui.py

# Prior plan: the ansi-dark band -> style palette the borders/colors read against
@.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core.sess_status_band + status-colored, zebra-striped sessions table</name>
  <files>claude_monitor/core.py, claude_monitor/test_claude_monitor.py, claude-tui.py</files>
  <read_first>
    - claude_monitor/core.py:684 (SESS_RANK), :846 (sess_rows), :833 (_safe_cell), :806 (fmt_elapsed) -- the session cells + the control-char mitigation being preserved
    - claude_monitor/test_claude_monitor.py:751-802 (tui session-rows asserts) -- where sess_status_band's asserts sit
    - claude-tui.py:189-202 (render_all sessions loop) and :199 (the Text(...)-per-cell markup mitigation, T-09-01) -- every cell stays a rich.Text
    - claude-tui.py:96-98 (on_mount DataTable setup: cursor_type, add_columns) -- where zebra_stripes is enabled
    - claude-tui.py:50-66 (CSS block) -- where any DataTable row style is declared
  </read_first>
  <behavior>
    - sess_status_band("waiting") == "yellow"; sess_status_band("running") == "green"; sess_status_band("done") == "dim" (fixed D-07 palette)
    - sess_status_band("") and sess_status_band("zombie") and sess_status_band(None) == a single neutral default token (unknown status is never a raise, matching sess_rank's rank-99 tolerance)
  </behavior>
  <action>
    Add a pure function sess_status_band(status) to claude_monitor/core.py returning the
    fixed status color-name token (D-07): "waiting" -> "yellow", "running" -> "green",
    "done" -> "dim", and any other/unknown/None status -> one neutral default token (e.g.
    "default"). It is a total lookup mirroring sess_rank's unknown-tolerance; core imports
    no third-party package. Add asserts to demo() under a new "# --- tui session band
    (TUI-09) ---" block for the three known statuses plus the unknown/empty/None default.
    In claude-tui.py: enable zebra striping on the sessions DataTable (set zebra_stripes on
    the widget, e.g. in on_mount alongside cursor_type/add_columns). In render_all's sess_rows
    loop, color each row by core.sess_status_band(status): apply the token as the rich.Text
    style on the row's cells -- CRITICALLY, keep every cell a rich.Text built from
    core.sess_rows output (the T-09-01 mitigation at :199): add color by passing the style to
    the Text, never by wrapping a value in a markup string. Use ANSI style names, not RGB, so
    "yellow"/"green"/"dim" read under the inherited ansi-dark palette. Sort order (waiting ->
    running -> done via SESS_RANK), columns (status/project/time), and the empty-state row
    are unchanged (D-07).
  </action>
  <verify>
    <automated>just selfcheck</automated>
    <human-check>`just tui` with live sessions: waiting rows read yellow, running green, done dim/gray, with subtle zebra striping between rows; a project directory containing markup like [bold] or a control byte renders literally/defused, not as styling and not as a crash.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def sess_status_band' claude_monitor/core.py` shows it defined in core.
    - `just selfcheck` exits 0 (sess_status_band asserts pass AND the existing _safe_cell / sess_rows control-char asserts at :773-802 still pass; core still stock-python3 importable).
    - render_all still constructs each sessions cell as a rich.Text from core.sess_rows output (the :199 mitigation): `grep -n 'add_row' claude-tui.py` shows the cells wrapped in Text(...), never bare strings; color is applied via Text style.
    - The DataTable has zebra striping enabled (source read of on_mount / CSS).
  </acceptance_criteria>
  <done>sess_status_band + asserts exist, `just selfcheck` exits 0, and the sessions table renders status-colored, zebra-striped rows with the Text-per-cell mitigation intact.</done>
  <reversibility rating="reversible">A pure lookup + local render styling; no migration, no contract change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: titled rounded bordered panels (CSS)</name>
  <files>claude-tui.py</files>
  <read_first>
    - claude-tui.py:50-66 (CSS block) -- #usage / #trends / #sessions currently use border-bottom: solid $panel; replaced by full rounded borders
    - claude-tui.py:79-86 (compose) -- the three panels (#usage Static, #trends Static, #sessions DataTable) that get border_title
    - claude-tui.py:88-102 (on_mount) -- where border_title may be set on each panel widget
  </read_first>
  <action>
    In claude-tui.py, give each of the three panels (#usage, #trends, #sessions) a rounded
    titled border (btop paneled layout, TUI-10): in the CSS block set a full rounded border
    (textual `border: round <color>`) on #usage, #trends and #sessions, replacing the current
    border-bottom: solid $panel rule, and set each panel's border_title (e.g. "usage",
    "trends", "sessions") via the widgets' border_title in on_mount or as a compose-time
    attribute. Border color is STATIC (D-08): use a fixed theme/ANSI color (e.g. $panel or an
    ANSI name), NOT band-coupled -- the threshold signal stays in the row text and gauges.
    Keep the existing layout intact: #usage / #trends height:auto, #sessions height:1fr and
    scrolling, the #body.stale dim, and the #coldstart sibling (never inside #body, so it is
    never dimmed) all unchanged. Borders read against the inherited ansi-dark palette.
  </action>
  <verify>
    <automated>just selfcheck</automated>
    <human-check>`just tui`: the usage, trends and sessions panels each appear as a titled, rounded bordered box (btop paneled layout); borders are a static color and do not change with usage level; the stale-dim and cold-start message still behave as before.</human-check>
  </verify>
  <acceptance_criteria>
    - The CSS block declares a rounded border on #usage, #trends and #sessions and each panel sets a border_title (source read of claude-tui.py CSS + on_mount/compose).
    - Border color is a static theme/ANSI color, not derived from core.band or any usage percentage (source read).
    - `just selfcheck` exits 0 and `just lint` is clean (core untouched; claude-tui.py change is CSS/render only).
  </acceptance_criteria>
  <done>All three panels render as titled rounded bordered boxes with static borders, and `just selfcheck` exits 0.</done>
  <reversibility rating="reversible">CSS-only change in the one textual file; no migration, no contract change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon unix socket -> claude-tui.py | The same read-only snapshot verb (0600, local); session `status` and `dir` field values reach the sessions render path. A project `dir` is an arbitrary, attacker-influenceable filesystem path -- the highest-value untrusted input on screen. No new boundary this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | sessions DataTable restyle over a hostile project `dir` | high | mitigate | The restyle MUST keep every cell a rich.Text built from core.sess_rows output (the :199 T-09-01 mitigation): a str cell runs through DataTable's markup parser (a dir named [bold]x is injection; [/] raises MarkupError inside a timer callback -> app exit), a Text takes the renderable passthrough. Color is applied as a Text style, never a markup string. core._safe_cell still strips C0/C1 control bytes (asserts at :773-775 stay green). Verified by selfcheck + a source assertion that add_row wraps each cell in Text(...). |
| T-10-05 | Denial of Service | render_all sessions loop under the 1s tick | medium | mitigate | sess_status_band is total (unknown status -> default, never raises); a render-time raise is caught by tick()'s except (claude-tui.py:170-178) and surfaced as "render error", not mislabelled "daemon unreachable". |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package install this plan: textual stays the only third-party dep, already lock-pinned (T-09-SC in force); sess_status_band is stdlib-only core. No legitimacy gate required. |
</threat_model>

<verification>
- `just selfcheck` exits 0 (sess_status_band asserts pass; existing _safe_cell / sess_rows mitigation asserts still green).
- `just lint` clean.
- Live TUI: status-colored zebra-striped sessions + three titled rounded bordered panels.
- Regression: a project dir containing markup or a control byte renders defused, never crashes the app.
</verification>

<success_criteria>
- TUI-09: the sessions table shows status-colored rows (waiting yellow / running green / done dim) with zebra striping, over the current plain DataTable, with the T-09-01 Text-per-cell mitigation intact.
- TUI-10: each of the three panels is a titled, rounded, bordered box with a static border (D-08).
- core.sess_status_band is pure, total, and never imports textual/rich.
</success_criteria>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift/source-grounding verification):
- `claude_monitor/core.py`: `sess_status_band(status)`
- `claude_monitor/test_claude_monitor.py`: new assert lines under demo() in block "# --- tui session band (TUI-09) ---" (no new function symbol)
- `claude-tui.py`: per-row status Text-styling + zebra_stripes on the sessions DataTable, and rounded titled border CSS + border_title on the three panels (render/CSS only, no formatter)
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/notifications-predictive-alerts/phases/10-tui-polish-btop-style/10-03-SUMMARY.md` when done
</output>
