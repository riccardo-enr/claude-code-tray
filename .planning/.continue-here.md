---
context: default
phase: v1.6-closeout
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-07-25T00:19:04+02:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints reflect the actual repository state and failures from this session.
> Acknowledge them before continuing.

- [ ] CONSTRAINT: Amend, do not re-archive — v1.6 is already archived and its
  phase directories are already under `milestones/v1.6-phases/`. Do not rerun
  milestone archival or delete requirements; update the existing records.
- [ ] CONSTRAINT: Preserve user files — do not edit, stage, or commit the
  unrelated untracked `SEED-005`, workstream `config.json`, or `.zed/`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Bulk patching after context drift | `apply_patch` hit the environment's `bwrap` failure; a later multi-file `git apply` failed on context and changed nothing. | advisory | Inspect exact current context and patch one file at a time; run `git diff -- <file>` immediately. |
| Invoking broken GSD CLI | `gsd-tools.cjs` fails because `/home/riccardo/.codex/package.json` is missing. | advisory | Complete the already-specified closeout manually; do not invoke `gsd-tools`. |

<current_state>

Pausing v1.6 milestone finalization after audit/stat gathering and before release
document edits. The user approved the existing audit override and confirmed
v2.0 Rust TUI as the next milestone.

`main` is two commits ahead of `origin/main` before this WIP handoff:

- `73d6017 fix(260724-wdy): show day in weekly projections`
- `7ba2c2b docs(quick-260724-wdy): record weekly projection fix`

The `v1.6` tag is still missing. No closeout document update from the interrupted
attempts was applied.

</current_state>

<completed_work>

- Weekly projection fix implemented and verified:
  - weekly reset/exhaustion output uses local `Day HH:MM`
  - 5-hour output remains `HH:MM`
  - `just selfcheck`, `just tui-selfcheck`, and `just lint` passed
- Quick task `260724-wdy` plan, summary, and STATE row committed.
- v1.6 readiness confirmed:
  - Phase 10 summaries: 3/3
  - requirements: 5/5
  - UAT: 4/4
  - security: 0 open
  - missing milestone audit already documented as an explicit override
- Final stats gathered:
  - git range `2fef6e9` through `73d6017`
  - 15 code commits
  - 28 files across source/planning, +2,143/-169
  - 2,872 application Python LOC
  - 6 phase-plan tasks plus 4 tracked quick-task tasks

</completed_work>

<remaining_work>

1. Apply the defined closeout updates, one file at a time:
   - `milestones/v1.6-ROADMAP.md`: include day-aware weekly timestamps and
     `just tui-selfcheck`.
   - `milestones/v1.6-REQUIREMENTS.md`: add the day-aware projection follow-up;
     move Rust TUI from deferred to a v2.0 next-milestone note.
   - workstream `MILESTONES.md`: extend range through `73d6017`, add stats,
     verification, weekly-day accomplishment, and v2.0 next step.
   - `.planning/RETROSPECTIVE.md`: add the follow-up gap/lesson and final stats.
   - `.planning/PROJECT.md`: change Rust TUI from proposed to confirmed v2.0.
   - workstream `ROADMAP.md`: add v2.0 Rust TUI as confirmed/not-yet-planned.
   - workstream `STATE.md`: record finalization/tag and v2.0 as next focus.
2. Mark the first three checklist items in
   `docs/plan/v1.6-milestone-finalization.md` complete.
3. Review the complete diff and ensure unrelated untracked files are absent.
4. Commit with a Conventional Commit such as
   `chore(release): finalize v1.6 milestone`.
5. Create annotated tag `v1.6` at the final closeout commit and verify it.
6. Mark the plan complete and move it to `docs/plan/completed/`.
7. Do not push the branch or tag without a separate user request/confirmation.

</remaining_work>

<decisions_made>

- Amend the existing v1.6 archive; do not rerun archive/delete operations.
- Proceed with the existing audit override, explicitly approved by the user.
- v2.0 is the Rust TUI rewrite; retain Python Textual as the parity oracle.
- Tag v1.6 only after the final release-record commit.

</decisions_made>

<blockers>

- GSD CLI is unavailable because its global install is incomplete.
- The patch helper is intermittent under the current sandbox; use small,
  verified edits rather than another bulk patch.
- No human action is currently required.

</blockers>

## Required Reading (in order)

1. `docs/plan/v1.6-milestone-finalization.md` — current checklist.
2. `.planning/workstreams/notifications-predictive-alerts/milestones/v1.6-ROADMAP.md`
   — existing release archive to amend.
3. `.planning/workstreams/notifications-predictive-alerts/milestones/v1.6-REQUIREMENTS.md`
   — final 5/5 requirement record.
4. `.planning/workstreams/notifications-predictive-alerts/MILESTONES.md` — v1.6
   release entry.
5. `.planning/PROJECT.md`, workstream `ROADMAP.md`, and workstream `STATE.md` —
   project and next-milestone state.
6. `.planning/RETROSPECTIVE.md` — existing v1.6 retrospective section.
7. `.planning/workstreams/notifications-predictive-alerts/quick/260724-wdy-weekly-projection-day/260724-wdy-SUMMARY.md`
   — final follow-up evidence.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run `milestone.complete` or `git rm REQUIREMENTS.md`; archival already happened.
- Do not use one large multi-file fallback patch. Patch and verify one file at a time.
- Do not include unrelated untracked files in staging.

## Infrastructure State

- No servers, watchers, subagents, or external async jobs are running.
- Branch: `main`.
- Remote: local branch was two commits ahead before this handoff commit.
- Tag `v1.6`: absent.

<context>

The intended closeout is an amendment that reconciles the already-shipped v1.6
archive with the last weekly-projection fix, then creates the missing annotated
tag. It is not a fresh milestone archive. The user plans to resume tomorrow and
then start v2.0 Rust TUI planning.

</context>

<next_action>

Start with: run `git status --short --branch`, read this handoff and the plan,
acknowledge the two blocking constraints, then patch
`v1.6-ROADMAP.md` alone and inspect its diff before touching the next file.

</next_action>
